import { LifecycleHook, VueRef, VueVersion, Watch } from "@yeger/vue-lib-adapter";

//#region src/index.d.ts
type NonEmptyArray<T> = [T, ...T[]];
type Column = number[];
interface Vue2ComponentEmits {
  (event: "redraw"): void;
  (event: "redraw-skip"): void;
}
interface Vue3ComponentEmits {
  (event: "redraw"): void;
  (event: "redrawSkip"): void;
}
interface HookProps<T> {
  columns: VueRef<Column[]>;
  columnWidth: VueRef<number | NonEmptyArray<number>>;
  emit: Vue2ComponentEmits | Vue3ComponentEmits;
  gap: VueRef<number>;
  items: VueRef<T[]>;
  maxColumns: VueRef<number | undefined>;
  minColumns: VueRef<number | undefined>;
  nextTick: () => Promise<void>;
  onBeforeUnmount: LifecycleHook;
  onMounted: LifecycleHook;
  rtl: VueRef<boolean>;
  scrollContainer: VueRef<HTMLElement | null>;
  ssrColumns: VueRef<number>;
  vue: VueVersion;
  wall: VueRef<HTMLDivElement>;
  watch: Watch;
}
declare function useMasonryWall<T>({
  columns,
  columnWidth,
  emit,
  gap,
  items,
  maxColumns,
  minColumns,
  nextTick,
  onBeforeUnmount,
  onMounted,
  rtl,
  scrollContainer,
  ssrColumns,
  vue,
  wall,
  watch
}: HookProps<T>): {
  getColumnWidthTarget: (columnIndex: number) => number;
};
//#endregion
export { Column, HookProps, NonEmptyArray, Vue2ComponentEmits, Vue3ComponentEmits, useMasonryWall };
//# sourceMappingURL=index.d.mts.map